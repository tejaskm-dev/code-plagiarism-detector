#!/bin/sh
#
# Starts the web UI + REST API on http://localhost:7070
#
#   ./bin/integrity-server [--port N] [--db path]
#
# A shorthand for `bin/integrity serve`, which owns the Java discovery logic.
set -eu

exec "$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd)/integrity" serve "$@"
