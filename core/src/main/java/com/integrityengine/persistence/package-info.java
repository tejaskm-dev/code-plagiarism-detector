/**
 * Repository contracts and their SQLite/JDBC implementations. Hand-written SQL only —
 * no ORM, so the storage layer stays inspectable and dependency-light.
 */
package com.integrityengine.persistence;
