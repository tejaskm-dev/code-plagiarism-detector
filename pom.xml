<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <!--
    io.github.<user> is the namespace Maven Central verifies against a GitHub account.
    Java package names stay com.integrityengine; the two are independent.
  -->
  <groupId>io.github.tejaskm-dev</groupId>
  <artifactId>integrity-engine-parent</artifactId>
  <!-- CI-friendly version: release builds pass -Drevision=X.Y.Z from the git tag. -->
  <version>${revision}</version>
  <packaging>pom</packaging>

  <name>Integrity Engine (parent)</name>
  <description>
    Source-code plagiarism detection for programming assignments: normalising tokenizers,
    winnowing fingerprints, and cohort-relative statistical flagging.
  </description>
  <url>https://github.com/tejaskm-dev/code-plagiarism-detector</url>

  <developers>
    <developer>
      <id>tejaskm-dev</id>
      <name>Tejas KM</name>
      <url>https://github.com/tejaskm-dev</url>
    </developer>
    <developer>
      <id>Sufiyan-Shiraj</id>
      <name>Sufiyan Shiraj</name>
      <url>https://github.com/Sufiyan-Shiraj</url>
    </developer>
    <developer>
      <id>sumedhaks8075</id>
      <name>Sumedha K S</name>
      <url>https://github.com/sumedhaks8075</url>
    </developer>
    <developer>
      <id>sreyaknair08</id>
      <name>Sreya K Nair</name>
      <url>https://github.com/sreyaknair08</url>
    </developer>
  </developers>

  <scm>
    <url>https://github.com/tejaskm-dev/code-plagiarism-detector</url>
    <connection>scm:git:https://github.com/tejaskm-dev/code-plagiarism-detector.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/tejaskm-dev/code-plagiarism-detector.git</developerConnection>
  </scm>

  <modules>
    <module>core</module>
    <module>app</module>
  </modules>

  <properties>
    <revision>0.1.0-SNAPSHOT</revision>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <javalin.version>6.3.0</javalin.version>
    <sqlite.version>3.46.1.3</sqlite.version>
    <junit.version>5.11.3</junit.version>
    <slf4j.version>2.0.16</slf4j.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.github.tejaskm-dev</groupId>
        <artifactId>integrity-engine-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>io.javalin</groupId>
        <artifactId>javalin</artifactId>
        <version>${javalin.version}</version>
      </dependency>
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-simple</artifactId>
        <version>${slf4j.version}</version>
      </dependency>
      <dependency>
        <groupId>org.xerial</groupId>
        <artifactId>sqlite-jdbc</artifactId>
        <version>${sqlite.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.2</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>3.5.1</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-source-plugin</artifactId>
          <version>3.4.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-javadoc-plugin</artifactId>
          <version>3.12.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <version>3.6.0</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <!-- Fails fast with a clear message instead of an obscure compiler error. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <id>enforce-toolchain</id>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <requireJavaVersion><version>[17,)</version></requireJavaVersion>
                <requireMavenVersion><version>[3.8,)</version></requireMavenVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!--
        Maven 3 installs and deploys POMs with ${revision} left unresolved, which makes a
        published artifact unresolvable. Flattening substitutes the real version.
      -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>flatten-maven-plugin</artifactId>
        <version>1.8.0</version>
        <configuration>
          <flattenMode>resolveCiFriendliesOnly</flattenMode>
          <updatePomFile>true</updatePomFile>
        </configuration>
        <executions>
          <execution>
            <id>flatten</id>
            <phase>process-resources</phase>
            <goals><goal>flatten</goal></goals>
          </execution>
          <execution>
            <id>flatten-clean</id>
            <phase>clean</phase>
            <goals><goal>clean</goal></goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!--
      Maven Central publishing: mvn deploy -Prelease -Drevision=X.Y.Z
      Needs a Central Portal token (server id "central") and a GPG signing key. The
      release workflow supplies both from repository secrets.
    -->
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>3.2.8</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals><goal>sign</goal></goals>
                <configuration>
                  <gpgArguments>
                    <arg>--pinentry-mode</arg>
                    <arg>loopback</arg>
                  </gpgArguments>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.sonatype.central</groupId>
            <artifactId>central-publishing-maven-plugin</artifactId>
            <version>0.11.0</version>
            <extensions>true</extensions>
            <configuration>
              <publishingServerId>central</publishingServerId>
              <autoPublish>true</autoPublish>
              <waitUntil>published</waitUntil>
              <!-- The application is shipped as a release jar, not as a dependency. -->
              <excludeArtifacts>
                <excludeArtifact>integrity-engine-app</excludeArtifact>
              </excludeArtifacts>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>
