/**
 * Immutable value/data types shared by every other package.
 *
 * <p>This is the only package whose types are {@code public} by default: they cross
 * every package boundary in the system. Everything else is package-private unless a
 * genuine cross-package need forces otherwise.
 */
package com.integrityengine.domain;
